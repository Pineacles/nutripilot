# Database
DB_USER=nutripilot
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=nutripilot

# Auth — generate with: openssl rand -hex 32
JWT_SECRET=CHANGE_ME_RANDOM_STRING
API_KEY=CHANGE_ME_RANDOM_API_KEY

# External APIs (optional)
USDA_API_KEY=

# CORS — your domain(s), comma-separated
CORS_ORIGINS=https://nutripilot.yourdomain.com

# Seed user (used by seed.py)
SEED_EMAIL=you@example.com
SEED_PASSWORD=CHANGE_ME
